<h1>All the people!!</h1>
<h2>Here are all the wonderful people you may or may not live with</h2>

<div>
    {{#if allProfilesData}}
    <div class="imgBox" >
        {{#each allProfilesData}}
            <a href="profile/{{user_id}}">
                <div class="profileImgBlock" onClick="something goes in here.">
                    <img class="profileImg"  src="/images/{{user_image}}" alt="{{name}}'s image'">
                    <h2 class="buttonAtBottom" >{{name}}</h2> {{!-- make sure this is included --}}
                </div>
            </a>
        {{/each}}
    </div>
    {{else}}
    <p>
        *silence*... <br> 
        There doesn't seem to be anyone here. Perhaps you live alone? In that case, I am sorry to say that this is not the app for you.
    </p>
    {{/if}}
</div>